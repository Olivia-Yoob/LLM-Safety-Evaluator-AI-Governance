# LLM Safety Evaluator & Governance — Project 3
# Python 3.11+ recommended. Pins reflect a verified-working environment.

# Core data / ML
pandas>=2.0
numpy>=1.26,<2.0
scikit-learn>=1.4
joblib>=1.3
matplotlib>=3.8

# LLM (Notebook 3): Claude API classifier
anthropic>=0.69
pydantic>=2.6          # structured output schema for the safety verdict
python-dotenv>=1.0     # load ANTHROPIC_API_KEY from a local .env (optional)

# Notebook runtime
notebook>=7.0          # or jupyterlab>=4.0
nbformat>=5.9
ipykernel>=6.29
